"use strict";Object.defineProperty(exports, "__esModule", { value: true });exports.default = void 0;


await jitiImport("dotenv/config");
var _config2 = await jitiImport("prisma/config"); // Prisma 6 no longer auto-loads .env when a config file is present, so the
// CLI (migrate/studio/seed) needs it loaded explicitly. Dev-time only — the
// running app gets its env from the host.
var _default = exports.default = (0, _config2.defineConfig)({ schema: "prisma/schema.prisma",
    migrations: {
      path: "prisma/migrations",
      seed: "ts-node prisma/seed.ts"
    },
    datasource: {
      url: process.env["DATABASE_URL"]
    }
  }); /* v9-1c4cce6f6af2cccd */
